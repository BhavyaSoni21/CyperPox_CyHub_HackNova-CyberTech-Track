# Backend environment variables — copy to .env and fill in values

# CORS: comma-separated list of allowed frontend origins
# Add your Vercel deployment URL here
CORS_ORIGINS=http://localhost:3000,https://cyper-pox-cy-hub-hack-nova-cyber-te-two.vercel.app

# Model path (relative to backend/)
MODEL_PATH=models/isolation_forest.pkl

# MongoDB — leave blank to use in-memory storage
MONGODB_URI=
MONGODB_DB=cyhub
